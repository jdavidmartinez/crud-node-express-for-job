<%- include('partials/_header'); -%>

<div class="containder">
    <div class="row">
        <div class="col-md-5 offset-md-3">
            <div class="card">
                <div class="card-body">
                    <form action="/edit/<%= location._id %>" method="POST">
                      <div class="form-group">
                          <input type="text" name="title" placeholder="Update a location" class="form-control" value="<%= location.title %>">
                      </div>  
                         <div class="form-group">
                             <textarea name = "description" placeholder = "Insert a description" cols = "80"  class = "form-control"><%= location.description %></textarea>
                           
                         </div>
                    <button type="submit" name="buttom" class="btn btn-primary btn-block">Update</button>    
        
                    </form>
                </div>
            </div>
        </div>
        
    </div>
</div>
<%- include('partials/_footer'); -%>